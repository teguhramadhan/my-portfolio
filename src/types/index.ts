export interface Skill {
  title: string;
  description: string;
}

export interface Work {
  title: string;
  category: string;
  year: number;
}

export interface NavigationItem {
  label: string;
  href: string;
}
